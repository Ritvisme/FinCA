# Copy to .env for local dev (optional — the app defaults to localhost:8000).
# In Vercel, set VITE_API_URL to your Render backend URL under Project Settings → Environment Variables.
VITE_API_URL=http://localhost:8000
